# requirements.txt

# Pinned versions to resolve the core conflict
tensorflow==2.13.0
keras==2.13.1
numpy==1.23.5

# Let the installer find the latest compatible versions for these
transformers
tokenizers
huggingface-hub
streamlit
Pillow
regex
